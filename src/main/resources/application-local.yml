server:
  spring:
    scheme: http
    host: localhost
    port: 3000
  react:
    scheme: http
    host: localhost
    port: 3000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/security?serverTimezone=Asia/Seoul
    username: admin
    password: admin1234

  jpa:
    hibernate:
      ddl-auto: create #create update none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  security:
    oauth2:
      client:
        registration:
          google: # /oauth2/authorization/google 이 주소를 동작하게 한다.
            client-id: 36541523365-f0nm80fpnjf3et4qshgkhdp027m40tnf.apps.googleusercontent.com
            client-secret: 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
            scope:
            - email
            - profile
            
          facebook:
            client-id: 3295784930650422
            client-secret: a2e0c77b4a0a76384f21aeb6fbec8693
            scope:
            - email
            - public_profile
          
          # 네이버는 OAuth2.0 공식 지원대상이 아니라서 provider 설정이 필요하다.
          # 요청주소도 다르고, 응답 데이터도 다르기 때문이다.
          naver:
            client-id: zn7XoH75HqCZ1HJUQL1G
            client-secret: tQg_5noAJ2
            scope:
            - name
            - email
            - profile_image
            client-name: Naver # 클라이언트 네임은 구글 페이스북도 대문자로 시작하더라.
            authorization-grant-type: authorization_code #oauth2 방식
            redirect-uri: http://localhost:8080/login/oauth2/code/naver

        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response # 회원정보를 json의 response 키값으로 리턴해줌.
  output:
    ansi:
      enabled: always

logging:
  level:
    com:
      twopow:
        security: trace